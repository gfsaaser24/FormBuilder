/**
 * Since we're no longer using Next.js, we're using CSS variables for fonts instead
 */

export const fontSans = {
  variable: "font-sans",
};

export const fontMono = {
  variable: "font-mono",
};